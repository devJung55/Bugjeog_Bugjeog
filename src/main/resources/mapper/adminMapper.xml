<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bugjeogbugjeog.app.bugjeogbugjeog.mapper.AdminMapper">

    <!-- 회원 전체 목록 -->
    <select id="selectAllMember" resultType="memberVO">
        select member_email, member_password, member_phone_number, memeber_name, member_register_date, member_status, member_img_original_name, member_img_uuid, member_img_path
        from tbl_member
    </select>

    <!-- 회원 조회 -->
    <select id="selectMember" resultType="memberVO">
        select member_email, member_password, member_phone_number, memeber_name, member_register_date, member_status, member_img_original_name, member_img_uuid, member_img_path
        from tbl_member where member_id = #{memberId}
    </select>

    <!-- 회원 삭제-->
    <delete id="deleteMember">
        delete from tbl_member
        where member_id = #{memberId}
    </delete>

    <!-- 회원 수정 -->
    <update id="updateMember">
        update tbl_member
        set member_email=#{memberEmail}, member_password=#{memberPassword}, memeber_name=#{memberName}, member_register_date=CURRENT_TIMESTAMP, member_status=#{memberStatus}, member_img_original_name=#{memberImgOriginal}, member_img_uuid='', member_img_path=''
        where member_phone_number='';
    </update>


</mapper>